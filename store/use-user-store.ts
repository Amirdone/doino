// create a store which is a state manager (like useState) but globally
// we are using zustand library

//import {create} from 'zustand'

//const useUserStore = create((set) => ({
   // user: {},
   // updateUser: (state) => set(() => -)
//}))